fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0
alembic==1.13.1
pydantic==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
python-multipart==0.0.9
httpx==0.27.0
groq==0.9.0
python-dotenv==1.0.1
faker==24.14.1
reportlab==4.2.2
# Pinned explicitly — these are pulled in transitively (by python-jose,
# sqlalchemy[asyncio], and uvicorn[standard]) with no version constraint,
# so pip was free to grab a brand-new release with no prebuilt Windows
# wheel yet, falling back to a from-source build that needs a C/Rust
# toolchain most machines don't have. These exact versions are known to
# ship cp311-win_amd64 wheels.
cryptography==42.0.5
greenlet==3.0.3
httptools==0.6.1
